# ─── BASE DE DATOS (Supabase / PostgreSQL) ───────────────
# Settings → Database → Connection string → Transaction pooler (puerto 6543)
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Settings → Database → Connection string → Session mode (puerto 5432)
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# ─── SUPABASE AUTH ────────────────────────────────────────
# Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL="https://[project-ref].supabase.co"

# Settings → API → Project API Keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJ..."

# Settings → API → Project API Keys → service_role (SECRETO — nunca exponer en frontend)
SUPABASE_SERVICE_ROLE_KEY="eyJ..."
